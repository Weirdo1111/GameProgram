Place a 4-frame horizontal zombie walk sheet here as `zombie_walk.png`.

The runtime loader uses this image for every enemy first, then falls back to the built-in Kenney/prototype sprites if the file is missing.
